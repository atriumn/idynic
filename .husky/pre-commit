#!/bin/sh

# Run lint-staged for linting
pnpm exec lint-staged

# Run TypeScript type checking for both apps
pnpm --filter web exec tsc --noEmit
pnpm --filter mobile exec tsc --noEmit

# Run tests
pnpm --filter web test:run
